import { TestScheduler } from 'jest';
import xxxxxx from '../src/calc.js';

describe('Calculator', () => {


  test('Should return the age submitted into Mercury equivalent', () {
    const age = 31;
    expect(calc.merc(age)).toEqual()


  })
})